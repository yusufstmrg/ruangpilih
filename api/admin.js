function unauthorized(req){return req.headers['x-admin-token']!==process.env.ADMIN_TOKEN}
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({ok:false,error:'method_not_allowed'});if(!process.env.ADMIN_TOKEN||unauthorized(req))return res.status(401).json({ok:false,error:'unauthorized'});try{const {sql}=await import('../lib/neon.js');const [a,b,c,d,e]=await Promise.all([sql`select count(*)::int n from rp_products where coalesce(editorial_status,'')<>'ARCHIVED'`,sql`select count(*)::int n from rp_products where status='VERIFIED'`,sql`select count(*)::int n from rp_events`,sql`select count(*)::int n from rp_events where event_name='search'`,sql`select count(*)::int n from rp_events where event_name='product_view'`]);const [f,g,h]=await Promise.all([sql`select count(*)::int n from rp_events where event_name='save_toggle' and (metadata->>'saved')='true'`,sql`select count(*)::int n from rp_events where event_name='affiliate_route_selected'`,sql`select count(distinct session_id)::int n from rp_events where session_id is not null`]);return res.status(200).json({ok:true,environment:process.env.VERCEL_ENV||'unknown',integrations:{database:Boolean(process.env.DATABASE_URL),auth:Boolean(process.env.NEON_AUTH_URL),ai:Boolean(process.env.GEMINI_API_KEY),affiliate:process.env.AFFILIATE_CONFIGURED==='true'},summary:{products:a[0].n,verified:b[0].n,events:c[0].n,searches:d[0].n,product_views:e[0].n,saves:f[0].n,route_clicks:g[0].n,users:h[0].n}})}catch(error){console.error(error);return res.status(503).json({ok:false,error:'admin_data_unavailable'})}}