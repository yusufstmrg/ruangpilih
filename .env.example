# Production integrations — store real values in Vercel Environment Variables only.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Set to true only after approved affiliate connectors are configured.
AFFILIATE_CONFIGURED=false
# Add connector-specific secrets only in Vercel, never in frontend files.
